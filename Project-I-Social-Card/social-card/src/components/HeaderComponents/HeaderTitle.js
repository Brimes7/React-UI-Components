import React from 'react';
import './Header.css';

const HeaderTitle = props => <h1> @Lambda </h1>;

const hTitle = props => {
    return(
        <div>
            <HeaderTitle />
        </div>
    );
};
export default hTitle;